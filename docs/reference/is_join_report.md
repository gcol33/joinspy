# Check if Object is a JoinReport

Check if Object is a JoinReport

## Usage

``` r
is_join_report(x)
```

## Arguments

- x:

  An object to test.

## Value

`TRUE` if `x` is a `JoinReport`, `FALSE` otherwise.
