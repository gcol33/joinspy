# Detect the appropriate join backend from input classes

Detect the appropriate join backend from input classes

## Usage

``` r
.detect_backend(x, y)
```

## Arguments

- x:

  Left data frame.

- y:

  Right data frame.

## Value

Character: "data.table", "dplyr", or "base".
